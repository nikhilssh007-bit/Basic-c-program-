#include <stdio.h>
int main() {
    int units;
    float bill;
    scanf("%d",&units);
    if(units<=100)
        bill = units * 5;
    else if(units<=200)
        bill = units * 6;
    else
        bill = units * 7;
    printf("Bill amount = Rs. %.2f", bill);
    return 0;
}
