#include <stdio.h>
int main() {
    //Your Code goes here!
    int level;
    scanf("%d",&level);
    if(level > 90)
    printf("overflow warning");
    else
    printf("safe level");
    return 0;
}
