#include <stdio.h>
int main() {
    //Your Code goes here!
    int length;
    scanf("%d", &length);

    if (length > 5)
        printf("Valid Email");
    else
        printf("Invalid Email");

    return 0;
}
