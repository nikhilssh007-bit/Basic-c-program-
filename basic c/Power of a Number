#include <stdio.h>
int main() {
    int base, exp;
    long long power = 1;
    scanf("%d %d", &base, &exp);
    for(int i = 1; i <= exp; i++) {
        power *= base;
    }
    printf("%lld", power);
    return 0;
}
