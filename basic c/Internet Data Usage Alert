#include <stdio.h>
int main() {
    //Your Code goes here!
    float data;
    scanf("%d",&data);
    if(data > 2.0)
    printf("data limit exceded");
    else
    printf("data within limit");
    return 0;
}
