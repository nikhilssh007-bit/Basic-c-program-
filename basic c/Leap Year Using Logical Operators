#include <stdio.h>
int main() {
    //Your Code goes here!
    int y;
    scanf("%d", &y);
    if((y%4==0 && y%100!=0) || (y%400==0))
        printf("LEAP");
    else
        printf("NOT LEAP");
    return 0;
}
